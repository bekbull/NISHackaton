CREATE TABLE grades(
    grade VARCHAR PRIMARY KEY
);

CREATE TABLE curators(
	name VARCHAR
);

CREATE TABLE timetable(
	monday VARCHAR,
	tuesday VARCHAR,
	webnesday VARCHAR,
	thursday VARCHAR,
	friday VARCHAR
);